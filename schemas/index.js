import { categoryType } from './categoryType.js';
import { collectionType } from './collectionType.js';
import { productType } from './productType.js';

export const schemaTypes = [categoryType, collectionType, productType];
